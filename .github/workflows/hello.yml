name: My first Workflow

on: 
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest
    steps: 
      - name: Print a message
        run: echo "Hello Manoj! GitHub Actions is running."